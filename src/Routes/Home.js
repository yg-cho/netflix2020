import React from 'react';
import PropTypes from 'prop-types';

const Home = props => {
    return (
        <div>
            <h1> HOME </h1>
        </div>
    );
};

Home.propTypes = {

};

export default Home;