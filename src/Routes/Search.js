import React from 'react';
import PropTypes from 'prop-types';

const Search = props => {
    return (
        <div>
            <h1>Search</h1>
        </div>
    );
};

Search.propTypes = {

};

export default Search;