import React from 'react';
import PropTypes from 'prop-types';

const Detail = props => {
    return (
        <div>
            <h1>Detail</h1>
        </div>
    );
};

Detail.propTypes = {

};

export default Detail;